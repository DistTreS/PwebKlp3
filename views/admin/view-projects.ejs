<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Projects</title>
    <link href="/public/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">

<div class="bg-white p-6 rounded shadow-md">
    <h1 class="text-2xl font-bold mb-4">Projects</h1>
    <table class="min-w-full bg-white">
        <thead>
            <tr>
                <th class="py-2 px-4 border-b border-gray-200 bg-gray-50">Project Title</th>
                <th class="py-2 px-4 border-b border-gray-200 bg-gray-50">Description</th>
                <th class="py-2 px-4 border-b border-gray-200 bg-gray-50">Start Date</th>
                <th class="py-2 px-4 border-b border-gray-200 bg-gray-50">End Date</th>
                <th class="py-2 px-4 border-b border-gray-200 bg-gray-50">Student</th>
                <th class="py-2 px-4 border-b border-gray-200 bg-gray-50">Supervisor</th>
            </tr>
        </thead>
        <tbody>
            <% projects.forEach(project => { %>
                <tr>
                    <td class="py-2 px-4 border-b border-gray-200"><%= project.title %></td>
                    <td class="py-2 px-4 border-b border-gray-200"><%= project.description %></td>
                    <td class="py-2 px-4 border-b border-gray-200"><%= project.start_date %></td>
                    <td class="py-2 px-4 border-b border-gray-200"><%= project.end_date %></td>
                    <td class="py-2 px-4 border-b border-gray-200"><%= project.student.username %></td>
                    <td class="py-2 px-4 border-b border-gray-200"><%= project.supervisor.username %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
    <a href="/homeadmin" class="mt-4 inline-block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Back to Home</a>
</div>

</body>
</html>
